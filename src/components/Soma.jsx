import React from 'react';
import Card from './Card';

const Soma = (props) => {
    return (

        <Card title="Soma" purple>
            <span>
                <strong>Resultado: </strong>
                <strong>15</strong>
            </span>
        </Card>
    );
}

export default Soma;